# Относительно эталонного решения:
# 1. Не указал постусловие для конструктора.
# 2. В методе get() в постусловии не уточнил, что следующий элемент берется из головы очереди
# 3. Не использовал статусы операций, соответственно не имплементировал методы получения статусов.
# 4. Не выделил отдельный метод для удаления элемента из головы очереди. В моем решении удаление и возврат элемента выполняется в методе get()
# 5. В своем решении реализовал очередь, как наследника от ParentList. В связи с этим необходимо пересмотреть сам ParentList для связанных списков.
