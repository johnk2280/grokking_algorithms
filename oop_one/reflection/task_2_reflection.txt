# 1. Не ответил на дополнительные вопросы.
# 2. Не указал постусловие для метода remove(), т.к. невнимательно прочитал
# задание.
# 3. В эталонном решении метод find() перемещен в команды, хотя я считаю, что
# этот метод относится к запросам, т.к. метод find() не меняет состояние объекта.
# 4. В своем решении не указал постусловие для метода find(). Однако так же не 
# согласен с постусловием в эталонном решении - считаю, что курсор должен быть 
# установлен на первый найденный узел с текущим значением.
# 5. В методе get() в своем решении не описал постусловие, т.к. считаю. что
# оно не нужно.
# 6. Так же в своем решении не описал запросы статусов - для меня не очевидна 
# их необходимость. т.к. для поверки/запросов состояний мы имеем методы-запросы.
