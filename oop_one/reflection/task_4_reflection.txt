# 1. За основу взял структуру DynArray из 1-го курса по алгоритмам, т.к.
# посчитал ее достаточной.
# 2. В эталонном решении не понятен смысл добавления метода put_right(),
# т.к. он в либо повторяет метод append(), либо метод insert(), только
# отсчет позиции для вставки происходит реверсивно с последнего элемента.
# Ни в том, ни в другом случаях никаких преимуществ на мой взгляд не дает.
# 3. В своем решении не стал указывать методы проверки состояний, т.к. не вижу
# их практического применения. В процессе реализации появляется
# вероятность упущения смены статуса.
